import 'package:flutter/material.dart';

class Lot17 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Berjaya Daftar Lot Parking"),
      ),
      body: Center(
      ),
    );
  }
}